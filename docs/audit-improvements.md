# 2026-09-11の監査反映

対象は `ozaken.ai`。本人の「修正を反映して」に対応する実装記録。監査の私的な解析データや匿名案件の対応表は公開しない。

## 反映内容

| 監査項目 | 実装・編集先 |
|---|---|
| 入口の提供範囲 | トップのヒーロー・title・description、共通ナビ、Personの説明を更新 |
| トップの長さと重複 | 活動領域と4形式を統合。ヒーローにも目的別リンク。最新ニュース8件は直後に維持 |
| 基調講演の選びやすさ | 対象・持ち帰る視点は常時表示。詳しい本文・論点・関連資料は展開式 |
| 統合ページの比較 | ページ内ナビを追従表示。実績を関連する研修・組織変革・顧問の近くへ配置 |
| 匿名実績 | 確認済み5件の実施内容を使用。本人確認の課題3点は一般的な相談例として別に提示 |
| 問い合わせ | 種別別のラベル・本文例、テーマ・製品の引き継ぎ、任意のテーマ選択 |
| OGP | 古い年なし数値を外し、活動全体を表す画像2枚へ。テンプレートとキャッシュ版も更新 |
| 依存関係 | Astro 7.3.2・Sharp 0.35.4等へ更新。Node 22.23.2、check用依存を明示 |
| 記事からの依頼 | 記事末尾から依頼ページ・相談フォームへ。本文があればリードの二重掲載を避ける |
| 無料資料 | トップから `content.ozaken.ai/#downloads` へ直接案内 |
| 社内共有 | 同じ依頼ページ内でテーマ別の印刷・共有リンクを用意。PDFの手動同期を増やさない |
| 視覚的な優先度 | コンパクトな目的別一覧、数字の2列、短いプロフィール。主要な条件は14px以上を目安に調整 |
| 表現 | 「だけ」「しか」を避け、何を支えるかを具体化。フォームは日本語ラベルを主にする |
| 本文への移動 | mainランドマーク・スキップリンク。背景エフェクトの対象セレクターも同期 |
| 入力エラー | aria-invalid・aria-describedbyとフォーカス移動で該当欄を案内 |
| 入力の最大長 | 画面・APIの上限を共有。上限超過は400で返し、無断切り捨てを防ぐ |
| 情報の自動スクロール | 組織名は静止した一覧。装飾の光・粒子は維持 |
| 再訪 | 完了済みのセッションでは開幕を省略。初回の見出し先行・光の散乱は維持 |
| 解析 | contact_cta / contact_start / contact_submitに固定の種別・テーマを記録。個人の入力は渡さない |
| ニュースの検索・共有 | カテゴリ・掲載年・キーワード、URLでの復元、戻る操作、0件案内 |
| 外部リンクの区別 | 外部ニュースは↗と読み上げ用の別タブ説明 |
| 継続的な検証 | build・check・フォームテスト・依存監査を実施。自動CI追加はGitHubのworkflow権限不足で未適用 |

## 検証の区別

ローカルのCMS未設定ビルドは記事なしで成功する。記事の残存、最新8件、カテゴリ、記事末尾の導線は、CMS設定のあるCloudflareプレビュー／本番で別に確認する。キーを取り出したり公開PRへ渡したりしない。

フォームは通信を模擬するテストとUIの入力確認を使う。実メールの送信や新しい自動返信は行わない。GA4イベントのコード実装と、管理画面でのカスタムディメンション登録・受信確認は別。

表示速度のスコアと、実際の有効相談・受注への効果は未確定。見た目の変更だけで改善率を保証しない。匿名案件ごとの期間・人数・担当範囲・成果も追加確認が必要。

## 回帰確認

- ヒーロー初回、同一セッションでの再訪、暗幕解除、見出しが先に出る順序。
- PC、390px、320pxの改行・はみ出し、本文・条件・相談ボタン。
- 背景の光と粒子がmainへの変更後も動くこと。LINEのキー操作と閉じる操作。
- 4形式とテーマの相談リンク、種別変更時の入力保持、エラーの関連づけ。
- ニュースのカテゴリと年月・語句の組合せ、戻る操作、0件表示。
- 3テーマの印刷対象と共有リンク。OGP2枚が1200×630で旧い数値がないこと。
- 旧8URLの転送、canonical、JSON-LD、匿名実績の公開範囲、最新ニュース8件。

運用の詳細は [統合ページ](speaking-page.md)、[フォーム](contact-email.md)、[解析](analytics.md)、[動き](design-and-motion.md)、[素材](content-and-assets.md)、[OGP](../scripts/ogp/README.md)を参照する。

## 実施した確認

2026-09-11、Node 22.23.2でbuild成功、checkは0 errors / 0 warnings（既存LINEコピーの非推奨ヒント1件）、8テスト成功、npm auditは検出0件。生成HTMLの内部リンク・アンカー、mainとH1、8件のJSON-LDを確認した。

PC・390px・320pxで表示を確認。フォームは研修テーマの事前選択、種別変更時の本文保持、未入力エラーの関連づけを確認。LINEのキー操作・終了、再訪時の開幕省略、main内の背景アニメーションも確認した。実送信はしていない。

3テーマの印刷CSSを一時的な画面プレビューで描画し、選択した1テーマと連絡先がA4の有効領域に収まることを確認した。実プリンターへの出力・ブラウザのPDF保存は未確認。共有ボタンのコピー先とOGP2枚の1200×630を確認した。
